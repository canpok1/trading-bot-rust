version: '3'
services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - configs/bot.env
      - configs/db.env
      - configs/slack.env
      - configs/exchange.env
    networks:
      - trading-bot-network
networks:
  trading-bot-network:
    external:
      name: trading-bot-network
